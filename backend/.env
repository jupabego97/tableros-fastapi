# ──────────────────────────────────────────────────────────────
# Tableros de Garantías - Variables de Entorno
# ──────────────────────────────────────────────────────────────
# Copie este archivo como .env y configure los valores necesarios.
# Los valores por defecto son adecuados para desarrollo local.

# ── Base de datos ──
# PostgreSQL (producción):
# DATABASE_URL=postgresql://user:password@localhost:5432/reparaciones
# SQLite (desarrollo):
DATABASE_URL=sqlite:///./tableros.db

# ── Entorno ──
ENVIRONMENT=development

# ── Autenticación JWT ──
# IMPORTANTE: Cambie este valor en producción
JWT_SECRET=change-me-in-production-nanotronics-2024
JWT_EXPIRE_MINUTES=480

# ── Admin por defecto (solo desarrollo) ──
CREATE_DEFAULT_ADMIN_ON_BOOT=true
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin123
DEFAULT_ADMIN_EMAIL=admin@nanotronics.com

# ── Registro público ──
ALLOW_PUBLIC_REGISTER=false

# ── CORS ──
# Separar múltiples orígenes con coma
# ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
ALLOWED_ORIGINS=

# ── Google Gemini (análisis de imágenes con IA) ──
GEMINI_API_KEY=

# ── Almacenamiento S3/R2 (imágenes) ──
USE_S3_STORAGE=false
S3_BUCKET=
S3_REGION=auto
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_ENDPOINT_URL=

# ── Socket.IO ──
# true = solo polling (recomendado en producción detrás de proxy)
SOCKETIO_SAFE_MODE=true

# ── Observabilidad ──
SENTRY_DSN=
SENTRY_TRACES_SAMPLE_RATE=0.0
ENABLE_PROMETHEUS_METRICS=true

# ── Debug (solo desarrollo) ──
EXPOSE_DEBUG_SCHEMA=false
RUNTIME_SCHEMA_MIGRATION=false
